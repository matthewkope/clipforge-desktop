{
  "name": "clipforge-desktop",
  "version": "0.1.0",
  "private": true,
  "description": "A desktop GUI wrapper for yt-dlp downloads.",
  "author": "ClipForge",
  "main": "dist/main/index.js",
  "scripts": {
    "dev": "vite --host 127.0.0.1",
    "electron:dev": "npm run build:main && concurrently -k \"npm run dev\" \"wait-on tcp:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .\"",
    "build": "npm run build:main && vite build",
    "build:main": "tsc -p tsconfig.main.json",
    "build:extension-icons": "node scripts/generate-extension-icons.mjs",
    "build:app-icon": "node scripts/generate-app-icon.mjs",
    "fetch-binaries": "node scripts/fetch-binaries.mjs",
    "install:extension-host": "node scripts/install-native-host.mjs",
    "app:install": "bash scripts/install-app.sh",
    "pack": "npm run build && npm run build:app-icon && npm run fetch-binaries && electron-builder --dir",
    "dist": "npm run build && npm run build:app-icon && npm run fetch-binaries && electron-builder",
    "preview": "vite preview --host 127.0.0.1",
    "test": "npm run build:main && node --test tests/*.test.mjs",
    "typecheck": "tsc --noEmit && tsc -p tsconfig.main.json --noEmit"
  },
  "dependencies": {
    "lucide-react": "^0.468.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "@types/react": "^19.2.6",
    "@types/react-dom": "^19.2.3",
    "@vitejs/plugin-react": "^5.0.4",
    "concurrently": "^9.1.0",
    "cross-env": "^7.0.3",
    "electron": "^39.2.6",
    "electron-builder": "^25.1.8",
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "typescript": "^5.7.2",
    "vite": "^7.2.4",
    "wait-on": "^8.0.1"
  }
}
